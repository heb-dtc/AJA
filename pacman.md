### Refresh keys after loooong time without udpdate
```bash
sudo pacman-key --refresh-keys
```

### remove package / remove package ignoring dependencies
```bash
sudo pacman -R <package>
```

```bash
sudo pacman -Rdd <package>
```

### clear cache
```bash
paccache --remove
```
