### check system wide disk space
`df -h`

### check space for specific partition
`du -h <partition-name>`

### check space for specific partition limiting depth
`du -h --max-depth=1 <partition-name>`
